const config = {
    pg: {
        pgUser: "admin",
        pgHost: "localhost",
        pgDatabase: "smart_building_monitoring",
        pgPassword: "DKhH3s3205Xadas",
        pgPort : 5432
    }
}

module.exports = config;
